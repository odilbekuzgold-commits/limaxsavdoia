export const dynamic = 'force-dynamic';
import { apiGet } from '../../../lib/api';
import { Empty, PageShell } from '../../../components/PageShell';
type Item={id:string;title:string;language:string;status:string;validUntil?:string|null};
export default async function KnowledgePage(){let items:Item[]=[];let error='';try{items=(await apiGet<{data:Item[]}>('/api/v1/knowledge')).data}catch(e){error=e instanceof Error?e.message:'Yuklanmadi'}return <PageShell title="Bilimlar bazasi" description="AI javoblari uchun tasdiqlangan va amal qiluvchi biznes ma’lumotlari.">{error&&<div className="data-error">{error}</div>}{items.length?<div className="table-wrap"><table><thead><tr><th>Sarlavha</th><th>Til</th><th>Status</th><th>Amal qilish muddati</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><b>{x.title}</b></td><td>{x.language}</td><td><span className="table-tag">{x.status}</span></td><td>{x.validUntil??'—'}</td></tr>)}</tbody></table></div>:!error&&<Empty>Tasdiqlangan bilimlar hali qo‘shilmagan.</Empty>}</PageShell>}
